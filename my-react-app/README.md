# Inventory Assist — Frontend

See the root [README](../README.md) for full setup instructions.

```bash
npm install
npm run dev
```

Frontend runs at `http://localhost:5173`.  
Requires the backend running at `http://localhost:3001` — see `server/.env.example`.
